name: Tests
on: [push]
jobs:
  tests:
    name: Tests
    runs-on: windows-2022
    steps:
      - name: Execute tests
        #shell: pwsh
        run: |
          Get-ChildItem -Path .
          whoami
